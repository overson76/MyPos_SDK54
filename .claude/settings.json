{
  "_comment": "프로젝트 settings — git tracked. 사장님 두 PC (Windows 본관 + macOS 별관) 모두 git pull 만 받으면 동일 hook 자동 적용. ~/.claude/settings.json 의 SessionStart/PreToolUse 는 제거 — 프로젝트 settings 가 단일 진실. Stop hook 은 ~/.claude/settings.json 에 일반 적용 유지.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/session-start-pull.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/pre-push-check.sh"
          }
        ]
      }
    ]
  }
}
