# 2026-06-04 (4부) — 별칭 등록 모달 입력 버그 (PC 타이핑 + 가로 키보드 가림)

## 한 줄 요약
PC 카운터에서 "손님 별칭 등록" 모달에 타이핑이 안 먹히고, 아이폰 가로에서 키보드가 "다음" 버튼을 덮던 두 사고를 한 번에 처방 — web 에서 `autoFocus` 끔 + 키보드 높이 구독해 sheet 를 키보드 위로 제한. PC/폰 둘 다 배포 완료, **내일 매장 실검증 대기**.

## 무엇이 바뀌었는가 (Q&A)

| 질문/이슈 | 결론 |
|---|---|
| (세션 시작) origin 풀 받았나? | 이 PC 는 origin/main 과 완전 일치(`1d642ef`). 받을 것 0. 단 NAS 베어 리포가 52커밋 뒤처져 있어 `git push nas main` 으로 따라잡힘(노트북이 NAS 경유로 받을 차례). |
| 라이브 URL 들어가면 매장 실제와 동일? | 조건부 예. 같은 매장(store)에 가입된 브라우저면 Firestore `onSnapshot` 양방향이라 실시간 동일 + **입력도 실제 반영**(구경 아닌 공동 편집). 미가입이면 AuthScreen. 단순 접속은 deploy:web 사고와 무관. |
| PC 별칭칸 타이핑 안 먹힘 원인 | 별칭 TextInput 의 `autoFocus` 가 RN-Web(Electron)에서 부모 `Pressable`/`KeyboardAvoidingView` 마운트와 충돌 → 포커스가 잡혔다 풀림. AddressBookModal 이 같은 이유로 autoFocus 를 뺀 선례. (강한 추정 — 내일 검증) |
| 아이폰 가로 키보드 가림 원인 | 모달 `sheet` 에 높이 제한이 없어 콘텐츠가 다 펼쳐지고 KAV 가 통째로 위로 밀어 헤더는 잘리고 버튼은 키보드 뒤. |
| 처방 | ① web 에서만 `autoFocus` 끔(폰 유지) ② `Keyboard.addListener` 로 키보드 높이 구독 → sheet `maxHeight` 를 키보드 위 영역으로 제한 + 상단 정렬 ③ `returnKeyType` 으로 별칭→전화칸→완료 |

## 신규/변경 파일
- `components/AliasPromptModal.js` — 유일 변경 (+52 −8). autoFocus 분기 / 키보드 높이 구독(`kbHeight`) / sheet `maxHeight` / `backdropTop` 스타일 / `returnKeyType` + `phoneRef`.

## 알려진 문제 / 미해결 (다음 세션 진입점)
- **PC 타이핑 원인은 `autoFocus` 추정** — AddressBookModal 선례 + "폰은 되고 web 만 안 됨" 증상 일치로 강한 확신이나, 내일 매장 PC 실검증 필요. 안 고쳐지면 다음 후보: sheet 의 `Pressable` 포인터 이벤트 / `react-native-gesture-handler` 간섭.
- 아이폰 OTA 는 runtime `1.0.2` 로 발행 — 폰 빌드가 1.0.2 이상이어야 받음(과거 1.0.1 불일치 사고 이력). 최근 OTA 들이 정상 적용됐으면 OK.

## 다음 세션 진입 가이드
```bash
git branch            # main = 8081bec (이번 fix 머지 완료)
git worktree list     # 작업 워크트리 claude/pensive-shamir-7ff56d
npm test              # 582 통과 확인
```
- 내일 매장 검증 결과 듣고: ① 둘 다 OK → 종결(필요 시 NAS/origin 정리) ② PC 안 되면 추가 원인 조사(위 후보).
- 검증 방법: **PC** = `Ctrl+Shift+R` 후 별칭칸 타이핑 / **아이폰** = 앱 2회 재시작 후 가로 키보드 + "다음" 버튼.

## 핵심 기술 결정 (왜)
- **autoFocus 를 web 만 끔**(`Platform.OS !== 'web'`): 폰은 자동 키보드 UX 가 좋아 유지, PC 는 버그 회피. 검증된 AddressBookModal 패턴.
- **KAV offset 대신 키보드 높이 직접 구독**: landscape 폰은 키보드가 화면 절반 이상 → offset 만으론 부족(메모리 2026-05-29 교훈). sheet 를 키보드 위 영역으로 제한해 내부 ScrollView 스크롤로 버튼 접근.

## 배포 / 빌드 명령
```bash
npm test                  # 582 통과
npm run deploy:web        # PC 라이브 — Version 9c771ebf 배포 완료
eas update --branch production --message "..."   # 폰 OTA — group 3440f9fb, runtime 1.0.2 발행 완료
```
