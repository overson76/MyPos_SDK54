# 2026-06-12 — 쓰기한도 사고 수습 · Blaze 전환 · 주소록 좀비 영구처방 (게이트+휴지통)

## 한 줄 요약
무료 쓰기한도(2만/일) 차단이 주소록 "수정해도 부활" 사고의 1차 원인 — Blaze 전환으로 즉시 복구, 이어서 부활 경로 3종(부팅 역류·자동 재등록·조용한 실패)을 게이트/휴지통/배너로 전부 봉인. 배포 7회.

## 무엇이 바뀌었는가 (Q&A)
| Q | 핵심 답/결정 |
|---|---|
| 주소록 수정이 자꾸 되돌아옴 | 쓰기한도 100% 소진(콘솔 실측 2만/2만) → 읽기만 되는 반쪽 동기화. 수정이 서버에 못 올라가고 재시작 때 옛 데이터가 덮음 |
| 연동이 또 풀림 | 같은 원인. 폭주는 6/11 아침 9~10시 시간당 1.5만 — 옛 번들 기기 루프 추정 |
| 근본 해결 | **Blaze 종량제 전환** (무료체험 $300/90일, 자동청구 없음) + 예산 알림 ₩10,000 (50/90/100%, 크레딧 차감 전 기준) |
| 삭제+재시작 후에도 부활 | ① 부팅 시 옛 로컬사본이 첫 pull 전에 push 되는 레이스 → **snapshotSeen 게이트** (1e7d09f) ② 주문 칸에 실린 옛 주소를 확정/결제가 재등록 → **휴지통 tombstone** (ee693f0): 삭제=표식, 24h 자동 재등록 차단, 수동 재등록은 허용 |
| 실패가 안 보임 | CloudHealthBanner 빨간 띠 + 실패분 30초 자동 재전송 (575c1d1) |
| 복원 EXE 무반응 | window.prompt 미지원 → 자체 병합/교체 오버레이. 75개 시드 버튼 제거 |
| 주방 요청 | 조리중 일괄 버튼 (3077570) + 카드 푸터 2단 (0a7bdae) + 그리드 3칸 고정 (cb2123f) |
| EXE 새로고침 | Ctrl+F5 안 먹힘(메뉴바 제거) — **재시작이 곧 새로고침** |

## 신규/변경 파일
utils/cloudHealth.js·useOrderFirestoreSync.js·useAddressBook.js, components/CloudHealthBanner.js·AddressBookPanel.js, screens/KitchenScreen(+styles), utils/orderReducer.js, __tests__ 2종. seedAddresses.js 삭제.

## 알려진 문제 / 다음 진입점
**CID 별칭 버그 2종 (미해결, 시뮬 재현 확보)** — ① 착신 배너가 저장된 단골(테스트1/010-9999-8888)을 번호로만 표시 ② 주문 확정 시 칸에 deliveryAlias/Phone 미기록 → 카드 라벨 공백. 메모리 `project_cid_alias_slot_bug_2026_06_12` 참조. 부속: 시연 잔재 entry 청소. 6/11 오전 매출 history 클라우드 미반영(CSV 보존 안내됨).

## 다음 세션 진입 가이드
main `ee693f0` (push 완료, 워크트리 lucid-meninsky-1c6836 동일 시점). `npm test` → 배포는 `npm run deploy:web` 후 `npx eas update --branch production` 순차. Firebase = Blaze (프로젝트 mypos-4cfcc, 결제계정 01D3C1-958B67-2A59AA).

## 핵심 기술 결정
- 휴지통 = addresses 문서 tombstone(deleted/deletedAt) — 전 기기 동기화로 인지, meta 양방향 sync(union 사고 패턴) 회피
- 게이트 = 컬렉션별 첫 snapshot 전 push 전면 금지 — "source of truth = 서버" 원칙 강제
- 재시도 = 실패 시 lastSynced 비전진 + 30초 retryTick (상태 변화 없인 재실행 없어 폭주 불가)
