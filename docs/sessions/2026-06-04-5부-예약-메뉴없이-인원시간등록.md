# 2026-06-04 (5부) — 예약: 메뉴 없이 인원·시간만으로 등록

## 한 줄 요약
"예약은 메뉴를 정하지 않고 시간·인원만 잡는 경우가 많다"는 사장님 요청 — 빈 예약 슬롯(예약1)을 탭하면 **인원(−/+)·시간 입력창**이 떠서 메뉴 없이 예약을 등록. PC/폰 배포 완료, **내일 매장 검증 대기**.

## 무엇이 바뀌었는가 (Q&A)

| 질문/이슈 | 결론 |
|---|---|
| 예약을 메뉴 없이 등록하고 싶다 | 빈 예약 슬롯 탭 → `ReservationQuickModal` (인원 −/+ + 시간 휠). [예약 등록]=메뉴 없이 점유 / [메뉴 담기]=저장 후 주문 화면 |
| 슬롯 "차 있음" 판정 | 기존 `confirmedItems.length>0` 만 봤음 → 예약은 `partySize>0 ∥ deliveryTime` 도 "예약됨" 으로 인정 |
| 인원 입력 방식 | **−/+ 버튼** (텍스트 입력 없음 → 4부의 PC 타이핑·가로 키보드 버그와 무관) |
| 시간 입력 | 기존 `TimeWheelPicker` 재사용 (모달 위 중첩) |
| 인원 필드 | `emptyOrder.partySize` 신규 + `orders/setReservationInfo` 액션 |
| 등록 후 표시 | 슬롯에 `📅 예약 / 👥 N명 / 📅 오후 7시` |

## 신규/변경 파일
- `components/ReservationQuickModal.js` (신규, 246줄) — 인원/시간 입력 모달 (absolute overlay 패턴)
- `utils/orderReducer.js` — `partySize` 필드 + `orders/setReservationInfo` 액션
- `utils/OrderContext.js` — `setReservationInfo` 래퍼 + value/noop 등록
- `screens/TableScreen.js` — 빈 예약 슬롯 탭 분기 + 예약 표시 + 모달 렌더
- `screens/TableScreen.styles.js` — `tileReservationBadge` / `tileReservationInfo`
- `__tests__/orderReducer.test.js` — 예약 액션 테스트 +5 (587)

## 알려진 문제 / 미해결 (다음 세션 진입점)
- **예약 취소/비우기 버튼 없음** — 잘못 등록 시 인원 0+시간 미정으로 다시 등록하면 빈 슬롯처럼 보이나, 명시적 "예약 취소" 버튼 미구현. 사장님 실사용 후 필요하면 모달에 추가.
- 주방 화면·영수증의 인원 표시는 이번 범위 밖 (예약 표 전용).

## 다음 세션 진입 가이드
```bash
git branch            # main = 632f690 (예약 기능) + 세션노트 커밋
npm test              # 587 통과
```
- 내일 매장 검증: **예약1 탭 → 인원/시간 → [예약 등록] → 표시 확인**.
- 안 되거나 추가 요구 시 보수 (특히 예약 취소 버튼 여부).

## 핵심 기술 결정 (왜)
- **−/+ 버튼 인원 입력**: 텍스트 입력을 피해 같은 세션 4부의 PC autoFocus·가로 키보드 가림 사고를 원천 차단.
- **빈 예약도 order 객체 점유**: `setReservationInfo` 가 메뉴 없이 order 생성 → 슬롯이 "예약됨" 으로 표시. 손님 오면 같은 슬롯에서 메뉴 추가(기존 주문 흐름 그대로 재사용).
- 슬롯 동적 확장(y1,y2,y3…)은 기존 `DYNAMIC_SLOT_PREFIX` + `compactSlotsByPrefix` 그대로.

## 배포 / 빌드 명령
```bash
npm test                  # 587 통과
npm run deploy:web        # PC — Version aa1b8cf6 배포 완료
eas update --branch production --message "..."   # 폰 OTA — group 4b3dc181, runtime 1.0.2
```
