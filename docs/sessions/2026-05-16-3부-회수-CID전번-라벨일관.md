# 2026-05-16 (3부) — 회수 후보 확장 + CID 전번 빠른 등록 + 배달 라벨 일관화

## 한 줄 요약

사장님 영업 효율 요청 3건 — (1) 조리완료 배달이 회수 차수에 진입하도록 확장, (2) CID 새 번호 → 기존 entry 빠른 phone 등록 prompt, (3) 배달 관련 모든 표시/음성을 별칭 > 전번 > 주소 우선순위로 일관화. 3 commit + 2 deploy:web + 2 OTA. jest 414 → 415.

## 무엇이 바뀌었는가

| # | 사장님 요청 | 핵심 답 | 커밋 |
|---|---|---|---|
| 1 | 결제완료가 아닌 **조리완료** 도 회수 차수에 진입 (후불 배달 결제 늦으면 누락) | `utils/deliveryReturns.js` 의 ACCEPT_STATUSES = Set(['paid','ready']) + `utils/OrderContext.js` 의 `getReadyDeliveries()` 어댑터(orders → history-like) + `DeliveryReturnScreen` 합쳐 전달 | `2c2197c` |
| 2 | CID 새 번호 → 별칭만 있고 전번 누락된 entry 에 빠른 등록 (편집 화면 안 들어가도) | `screens/OrderScreen.js` 의 배달 헤더 inline chip "📞 번호 → 별칭 등록". phoneRegisterTarget useMemo + handleRegisterPhone. 조건 만족 시만 등장 | `2c2197c` |
| 3 | 배달 관련 모든 표시/음성/회수 라벨이 별칭 > 전번 > 주소 일관 우선순위 | `formatDeliveryLabel` 통합 헬퍼(phoneStyle 'full'/'short'/'spoken'). notify.js 에 `_addressBookCache` + `setNotifyAddressBook` 모듈 캐시 → speak* 함수가 별칭 음성. KitchenScreen 배달지 + 회수 차수 라벨도 헬퍼 적용 | `fb4cb40` |

## 신규/변경 파일

**신규 export**:
- `utils/addressBookLookup.js`: `formatDeliveryLabel({alias, phone, label}, opts)` — opts.phoneStyle: 'full'/'short'/'spoken'
- `utils/notify.js`: `setNotifyAddressBook(book)` — 모듈 캐시 sync API
- `utils/OrderContext.js`: `getReadyDeliveries()` — orders → history-like 어댑터

**변경**:
- `utils/deliveryReturns.js` — ACCEPT_STATUSES 확장 + ranked label 헬퍼 사용
- `utils/notify.js` — speak* 4개 함수 모두 `spokenDeliveryLabel(order)` 호출
- `utils/OrderContext.js` — useEffect 로 addressBook → notify 캐시 sync
- `screens/DeliveryReturnScreen.js` — readyDeliveries + history 합쳐 computeDeliveryReturns
- `screens/OrderScreen.js` — phoneRegisterTarget 헤더 chip
- `screens/OrderScreen.styles.js` — phoneRegisterChip 그린 스타일
- `screens/KitchenScreen.js` — 배달지 표시 헬퍼 적용
- `__tests__/deliveryReturns.test.js` — ready 케이스 +1

## 음성 정책 (사장님 메모리 일치 + 새 규칙)

- **별칭** = 사장님 식별자 → **항상 음성** (toggle 무관)
- **전번 / 주소** = 손님 개인정보 → **`speakAddress` 토글 ON 시만** (기본 OFF)
- 우선순위는 alias → phone(공백 분리, TTS 자연 발음) → address

## 다음 세션 진입 가이드

```bash
git checkout main && git pull
git log --oneline -5
# fb4cb40 (HEAD) feat(delivery): 배달 라벨 + 음성 일관 적용
# 2c2197c feat(returns/orders): 조리완료 회수 + CID 전번 빠른 등록
# 91ec965 fix(addr): PC 모달 close 회귀 + dedup
# ...

npm test                  # 415/415
npm run deploy:web        # 영업 외 시간만
npx eas update --branch production --message "..."
```

## 핵심 기술 결정

1. **음성 라벨 = 모듈 캐시 패턴** — 호출부 7군데(speakOrder 등) 시그니처 변경 X. OrderProvider 가 addressBook 변경 시 한 번만 setNotifyAddressBook 호출.
2. **getReadyDeliveries 가 history schema 와 통일** (`paymentStatus: 'ready'`) → deliveryReturns 의 필터 한 줄(Set 확장) 만 변경, 나머지 로직 무영향.
3. **CID 전번 chip 은 한 useMemo 로 4조건 검증** — 매 렌더 안전, 조건 빗나가면 null 반환 → chip 안 보임.
4. **헬퍼 phoneStyle 옵션** — 자주 칩(좁음) 'short', 일반 표시 'full', TTS 'spoken' — 한 정책 다른 사용처에 동시 적용.

## 후속 작업 후보

- **PC 영업 시간 자동화** (별도 큰 작업) — 옵션 A (절전 모드 + 작업 스케줄러). 사장님 권한 후 진행.

## 빌드 / 실행 명령

```bash
npm test                                # 415/415
npm run web                             # dev preview 8082
npm run deploy:web                      # 라이브 URL 갱신
npx eas update --branch production      # 폰 OTA
```
