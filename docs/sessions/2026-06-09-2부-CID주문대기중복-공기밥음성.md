# 2026-06-09 (2부) — CID 주문대기 슬롯 중복 수정 + 공기밥 음성 알림

## 한 줄 요약
전화 1통에 "주문대기" 슬롯이 d2·d3 로 **중복** 생기던 사고를 3겹(멱등 가드 + dismiss Firestore 삭제 + claim)으로 막고, **만두백반/공기밥 조리완료 시 "공기밥 확인해 주세요" 음성**을 추가. **진실보석 팝업 반복** 버그는 `efaf402`(다른 PC 작업)가 이미 수정돼 있던 것 → 이번 배포로 PC·폰에 반영.

## 무엇이 바뀌었는가 (Q&A)

| # | 이슈 (사장님 보고) | 원인 | 처방 |
|---|---|---|---|
| 1 | 배달 전화 오면 주문대기 슬롯 2개 중복 | `dismiss` 가 로컬 화면만 끄고 Firestore `incomingCall` 문서를 안 지움 → ① 멀티기기(PC·아이패드)가 각자 10초 타이머로 stash, ② 단일기기도 재수신 시 알림 부활 | **3겹** ① `submitPendingAsType` 멱등 가드 — `findPendingCallSlot` 으로 같은 발신자의 주문대기 슬롯 있으면 재사용(전화 1통=슬롯 1개) ② `dismiss` 가 Firestore 문서 삭제 → 모든 기기 전파 + 재수신 차단 ③ 자동 stash 가 stash 직전 dismiss(claim) → 레이스 완화 |
| 2 | 만두백반 공기밥 따로 챙기는 걸 빠뜨림 / 공기밥 추가 주문도 | 조리완료 음성에 공기밥 안내 없음 | 조리완료 항목이 만두백반/공기밥이면 기존 음성 끝에 "공기밥 확인해 주세요" **합쳐서** 발화(`cancelSpeech` 때문에 연속 speak 불가 → tail). `isSideRiceMenu` 는 **풀네임**으로 판정(만두백반 shortName='만백') |
| 3 | 진실보석(남자사장님) 팝업 계속 뜸 — 수정 반영됐나? | `needsAliasPrompt` 가 entry.alias 만 봐서 alias 비고 label="진실보석…"인 단골을 미상 오판 → 매번 모달 | **이미 수정됨**(`efaf402`, label 도 식별 인정). 신규 작업 아님 — 방금 배포(dd60b14/0be5937)로 반영. PC Ctrl+F5 / 폰 2회 재시작 필요 |

## 신규/변경 파일
**이슈 1 (CID 중복, 커밋 `dd60b14`)**
- `utils/orderHelpers.js` — `findPendingCallSlot` 신규 (같은 발신자의 메뉴 없는 주문대기 슬롯 찾기. phone 정규화 우선, 없으면 alias/address)
- `utils/OrderContext.js` — `submitPendingAsType` 멱등 가드 (migrateCart=false 전화 흐름 + 발신자 정보 있으면 기존 슬롯 재사용)
- `utils/useIncomingCall.js` / `.web.js` — `dismiss` 가 로컬 setCall(null) + Firestore incomingCall 문서 delete (simulate noop)
- `App.js` — 자동 stash 타이머가 dismiss(claim) 먼저 → submitPendingAsType
- `screens/OrderScreen.js` — 주소록 onSelect 가 발신번호를 entry 에 `mergePhoneIntoEntry`(entry.phone 유무 무관, 멱등). 옛 코드는 phone 없을 때만 등록해 단골 누락
- `__tests__/orderHelpers.test.js` — `findPendingCallSlot` 11 케이스

**이슈 2 (공기밥 음성, 커밋 `0be5937`)**
- `utils/notify.js` — `speakPartialReady`/`speakFullReady` 에 `sideRice` 옵션 (기본 false, 하위호환)
- `screens/KitchenScreen.js` — `isSideRiceMenu` 판정 + 항목별 토글(부분/전체) + "조리완료" 버튼 두 경로 연결

## 알려진 문제 / 미해결 (다음 세션 진입점)
- **멀티기기 동시 stash 중복은 실제 두 기기(PC+아이패드)로만 최종 검증 가능** — 매장 실검증 대기. 멱등 가드 자체는 jest 검증됨.
- 공기밥 **항목 자체** 조리완료 시 "공기밥 먼저 나왔습니다. 공기밥 확인해 주세요" 약간 중복 — 어색하면 멘트 다듬기(보수 백로그).
- 진실보석 — 사장님 실기기에서 Ctrl+F5/재시작 후 팝업 안 뜨는지 확인 필요.

## 다음 세션 진입 가이드
```bash
git branch          # main (HEAD 0be5937), 별관 claude/priceless-satoshi-3f5784 동일
npm test            # jest 630 통과 확인
npm run deploy:web  # PC 라이브
cd C:/MyProjects/MyPos_SDK54 && npx eas update --branch production --message "..."  # 폰 OTA
```
- 매장 실검증 결과(전화 중복 0건? 공기밥 음성?) 받아 보수 커밋으로 이어감.

## 핵심 기술 결정
- **중복 방어의 메인 = 멱등 가드**(순수 함수, jest 11케이스). dismiss 삭제·claim 은 보조(레이스 완화). 다층 방어로 단일·멀티기기 모두 커버.
- phone 매칭은 **phone 있으면 phone 만으로 판정**(다른 번호=다른 손님). phone 없는 발신자만 alias/address 폴백.
- 공기밥 음성은 `cancelSpeech` 구조상 별도 발화 불가 → 기존 음성에 **tail 합침**.
- 진실보석은 신규 작업 아님 — 이미 코드에 있던 fix 를 배포로 반영(efaf402 ⊂ dd60b14 확인).

## 배포 결과
| 작업 | 커밋 | deploy:web | OTA |
|---|---|---|---|
| CID 중복 | `dd60b14` | `5e76a863` | `8f5f1b4a` |
| 공기밥 음성 | `0be5937` | `c1536d8c` | `bde2f736` (Runtime 1.0.2) |

라이브: https://mypos-sdk54.overson76.workers.dev
