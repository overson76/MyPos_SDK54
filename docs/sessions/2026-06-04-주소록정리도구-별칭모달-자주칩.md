# 2026-06-04 — 주소록 자동 정리 도구 + 별칭 모달 fix + 자주칩 제거

## 한 줄 요약

영업 중 사장님 신고 3건 → ① 별칭 모달에 placeholder 주소가 별칭칸으로 새던 버그 fix ② 주문화면 "자주" 칩 제거 + 주소/전화칸 넓힘 ③ **주소록 자동 정리 도구 신규** (클릭 한 번으로 중복/오타 통합). main HEAD = b9ae89c, jest 1071/1071.

## 무엇이 바뀌었는가 (Q&A)

| # | 질문/신고 | 처방 |
|---|---|---|
| 1 | 별칭 등록 모달의 별칭칸에 "(주소 미입력) 010-..." 가 들어가 있다 | OrderScreen 호출부에서 initialAlias 가 placeholder 면 빈 값 + 전번은 전화칸으로 + currentAddress 도 placeholder 면 빈 값 |
| 2 | "자주" 칩 CID 구축으로 불필요. 빼고 주소/전번칸 넓게 | AddressChips 제거 + deliveryInputCompact flex(최소160) + 전화칸 165px |
| 3 | 앱 내부에 주소록 정리 기능. 클릭하면 오타/실수 정리 | 🧹 정리 버튼 + 분석 모달 + 통합. 247건 검증 케이스 커버 |

## 신규/변경 파일

### ① 별칭 모달 placeholder (dce54e2)
- [screens/OrderScreen.js](../../screens/OrderScreen.js) AliasPromptModal 호출부 — initialAlias/currentPhone/currentAddress 3곳 placeholder 방어. 모달 컴포넌트는 정상, 호출부 props 문제였음.

### ② 자주칩 제거 + 레이아웃 (0a36c95)
- [screens/OrderScreen.js](../../screens/OrderScreen.js) — AddressChips JSX + import 제거.
- [screens/OrderScreen.styles.js](../../screens/OrderScreen.styles.js) — deliveryInputCompact width 130 고정 → flex(최소 160), 전화칸 130→165, 글씨 fp(12)→fp(13).

### ③ 주소록 자동 정리 도구 (b9ae89c) ★ 신규 기능
- [utils/addressBookCleanup.js](../../utils/addressBookCleanup.js) (순수 함수):
  - `findPhoneDuplicates`: 같은 전번 흩어진 그룹. survivor = 주소>별칭>주문횟수. phone 필드 없이 label/key 텍스트 전번도 스캔.
  - `findSimilarAliasPairs`: 부분 포함 상호 쌍.
  - `mergeEntries`/`applyMerges`: survivor 에 전번/횟수/별칭/주소 흡수 후 삭제.
- [components/AddressBookCleanupModal.js](../../components/AddressBookCleanupModal.js): 분석 모달. 같은 번호 기본 ON(안전), 비슷 상호 기본 OFF. absolute overlay.
- [components/AddressBookPanel.js](../../components/AddressBookPanel.js): 🧹 정리 버튼 + 모달 + Toast.
- 테스트: addressBookCleanup 18 + 모달 import 스모크 1.

## 핵심 기술 결정

### 정리 도구 survivor 우선순위
`entryScore` = 진짜 주소(+100000) > 별칭(+10000) > 주문횟수(×10) > firstSeenAt tie-break. 정보 풍부한 entry 를 살리고 나머지 흡수.

### 같은 번호 = 안전(ON) / 비슷 상호 = 판단(OFF)
같은 전화번호는 같은 손님 확실 → 기본 통합. 비슷한 상호("탑마트" vs "신평 탑마트")는 다른 가게일 수 있어 사장님이 "같은 곳" 체크 시만.

### 폰에서 정리 → PC 자동 반영
정리는 setAddressBook → Firestore `stores/<sid>/addresses` write → PC onSnapshot 자동 sync. PC 라이브 URL 코드 갱신 없이도 데이터 정리 반영.

### 컴포넌트 검증 — import 스모크
react-test-renderer 미설치(CLAUDE.md 명시). jest-expo 가 모달 import 시 JSX 트랜스폼+import 체인 평가 → 문법/import 오류 잡음. 실제 렌더는 폰/PC.

## 사장님 매장 247건 검증 케이스

| 케이스 | 도구 동작 |
|---|---|
| 아가맘 010-2772-4064 | "아가맘" survivor (주문 4회) |
| 사하자원 010-2018-5492 | "사하자원"(주문 5회) survivor, 대성빨래(오기입) 흡수 |
| 해신슈퍼/해신수퍼 | 주소 있는 "해신슈퍼" |
| 탑마트/신평탑마트/탑마트1426 | 비슷 상호 → 체크 시 통합 |
| 영진정보/BHC/진실보석 | 비슷 상호 → 체크 시 통합 |
| 코덱스 010-5793-2079 | ⚠️ phone 필드에 5596-7008 오염 — 모달서 확인 or 수동 ✏️ |

## 알려진 문제 / 미해결

- **PC 라이브 URL 미반영** — wrangler 인증 만료. dce54e2/0a36c95/b9ae89c 3개 처방 PC 미반영. `npx wrangler login` 후 `npm run deploy:web` 필요. (단 정리 데이터는 폰에서 하면 PC 도 sync)
- 코덱스 phone 오염 (5596-7008) — 정리 도구가 두 그룹으로 볼 수 있음. 사장님 수동 확인.
- 정리 도구 실제 247건 첫 실행 — 폰 검증 필요.

## 다음 세션 진입 가이드

```bash
git branch          # main
git log --oneline -3 # b9ae89c HEAD
npm test            # 1071 통과
```

배포: 폰 OTA 3개 완료 (f06f1701 별칭모달 / 1a1bdbc4 자주칩 / 410fb1d9 정리도구). **PC 라이브 URL 은 wrangler 재인증 후 deploy:web** 필요.

## 빌드/실행 명령

```bash
npm test                                          # jest 1071/1071
npx wrangler login                                # PC 반영 전 1회 (브라우저 Allow)
npm run deploy:web                                # PC 라이브 URL
eas update --branch production --message "..."    # 폰 OTA
```

## 한 줄로 다음 세션 시작하려면

> main(f5155d4) + 정리 도구 실제 247건 폰 검증 + PC 라이브 URL wrangler 재인증 후 deploy:web.

---

# 후속 — 2부 (당일, preview 검증 중 발견)

사장님이 localhost preview 로 검증하다가 추가 신고 2건.

## ④ 주문화면 헤더 — 전화칸 제거 + 주소칸 안내 (f5155d4)
- 사장님: "(주소 미입력) 전번 칸에 직접 기입할 게 없다. [주문] 누르면 별칭/전번 팝업 뜨니 안내를 넣자. 우측 전화칸도 팝업에 이미 뜨는데 불필요."
- 처방: 주소칸이 "(주소 미입력)..." placeholder 면 값 대신 **파란 안내** "👉 [주문] 누르면 별칭·발신전화번호 입력창이 팝업됩니다". 우측 전화번호 입력칸(deliveryPhoneInput) 제거 — 전번이 주소칸+팝업에 이미 표시되어 중복.

## ⑤ 🔴 팥죽 대 옵션이 보통에 박히던 버그 (f5155d4)
- 사장님: "팥죽 대1+보통1 상황에서 대를 선택하고 면적게를 누르면 보통에 박힌다."
- 원인: `splitOffWithOptionToggle`(orderReducer)가 옵션 분리 시 새 slot 의 `largeQty` 를 **항상 0(보통)** 으로 생성. 대(large) portion 선택 정보(isLarge)가 행 키→sizePrompt→reducer 까지 전달 안 됨.
- 처방 (5곳 연결): `selectedRowKey`의 portion('large') → `selectedIsLarge` → `sizePrompt.isLarge` → `splitOffWithOptionToggle(isLarge)` → reducer 가 대 portion 에 옵션 적용 + 원래 largeQty 차감. 분리 수량도 대/보통 각 한도로 clamp.
- 교훈: **slot 의 대/보통(largeQty/qty)은 itemSplit 표시상 별 행이지만 같은 slot. 행 단위 작업(옵션/메모)은 행 키의 portion 을 reducer 까지 전달해야 한다.**
- 테스트 3 (대 분리/보통 분리/한도 clamp). jest 1074/1074.

## 후속 배포
- commit f5155d4. main HEAD f5155d4.
- 폰 OTA group ddaf9088 ✅. **PC 라이브 URL — wrangler 만료로 미반영** (dist/ 빌드는 성공, 업로드만 실패 → `npx wrangler login` 후 재시도).
