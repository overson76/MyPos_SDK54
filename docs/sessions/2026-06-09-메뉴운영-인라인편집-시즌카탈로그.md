# 2026-06-09 — 메뉴 운영 기능 묶음 (인라인 편집 + 시즌 카탈로그 + 진실보석 버그)

## Q&A

| # | 질문/요청 | 답/결정 |
|---|---|---|
| 1 | 진실보석 남자 사장님 팝업 계속 열림 — 버그냐? | ✅ 버그. needsAliasPrompt 가 entry.alias 만 봐서 alias 비고 label 만 있는 단골을 미상 오판 → 매번 모달. label 도 식별로 인정하게 fix. |
| 2 | 만두백반 대 옵션 없음 (대 1000원), 콩국수 대 적용됐나 (2000원) | 만두백반 sizeGroup 'milkalsu'(1000) 추가. 콩국수는 이미 'deulpatkong'(2000) + 국수/만백 — 확인. |
| 3 | 관리자 안 거치고 주문탭에서 바로 메뉴 추가/수정(가격/옵션/이미지) | [➕ 추가] 버튼 + MenuQuickEditModal 확장 (카테고리/대옵션/이미지). |
| 4 | 만두/공기밥 메뉴를 국수/만백 탭으로 드래그 이동 가능? | 안 됐음(즐겨찾기 탭만). 모든 카테고리 탭 드롭으로 확장(B). |
| 5 | 주문 추가 메뉴와 관리자 메뉴 중복 없어야 + 같은 이름이면 선택. 메뉴목록=시즌 마스터, 주문=현재 운영분 (여름 콩국수 꺼내고 겨울엔 주문서만 빠지고 목록 잔류) | 카탈로그/시즌 구분 도입. 중복 방지 + 불러오기 + 주문에서 빼기(시즌 종료). |

## 진행 흐름 (커밋 6 + 배포)

### ④ 진실보석 팝업 반복 버그 (`efaf402`)
- `entryIdentityName(entry)` — alias 우선, 없으면 label 도 식별(단 `__phone:`/`(주소 미입력)` placeholder 제외).
- `computeNeedsAliasPrompt({tableType,deliveryAlias,deliveryAddress,phone,addressBook})` 순수 함수 추출 — OrderScreen 인라인 30줄 대체.
- jest +13 (addressBookLookup 27).

### ② 만두백반 대 옵션 (`6e6f089`)
- menuData 만두백반 → `sizeGroup:'milkalsu', sizeUpcharge:1000`. 콩국수 이미 정상.
- 전파: mergeMenuItems 가 Firestore 미보유 필드는 default 유지.

### ③ 주문탭 인라인 편집 (`e52aa87`)
- 카테고리 헤더 [➕ 추가] 초록 버튼 (추천/즐겨찾기 제외).
- MenuQuickEditModal 풀 편집: 카테고리 칩 / 대옵션 토글+추가금+프리셋 / 이미지 선택+미리보기.
- utils/pickMenuImage.js 공유 util. addNewItem/At sizeGroup 통과.

### B 카테고리 간 드래그 이동 (`124de6e`)
- 탭별 ref + window 좌표 측정. hitTest 모든 비활성 탭 → 'FAV_TAB'/'CATTAB:<cat>'.
- 네이티브 PanResponder + 웹 HTML5 DnD 양쪽. 추천 탭 드롭 제외.
- 곁다리 fix: toggleFavorite useMenu destructure 누락 → 즐겨찾기 드롭 dead code 였음. 살림.

### 시즌 카탈로그 (`5bd273f`)
- "주문 노출" = 즐겨찾기 제외 그리드 배치. items=마스터 카탈로그.
- utils/menuCatalog.js (jest 13): collectInOrderIds / findItemByName / catalogOnlyItems.
- MenuContext: removeFromOrder(그리드만 제거) / addExistingToOrder / inOrderIds.
- MenuQuickEditModal: 중복 이름 배너+불러오기 / 카탈로그 picker / 시즌종료 vs 완전삭제 2버튼.
- SettingScreen: "🟢 주문 노출 / ⚪ 시즌 대기" 토글.

## 검증
- jest **619/619** (신규 menuCatalog 13 + addressBookLookup 13).
- preview(웹): ➕추가 모달, 만두백반 편집 대 ON 1000, 고기만두 카테고리 드래그,
  콩국수 시즌 종료 → 카탈로그 picker → 복귀 — 전부 동작 확인.

## 배포 (풀세트, 1.0.54)
- (아래 배포 결과 섹션에 사후 기록)

## 다음 체크리스트
- [ ] 사장님: 실제 매장에서 만두백반 음료 중복 → 편집모달 완전삭제 또는 드래그로 정리
- [ ] 시즌 메뉴 운영: 겨울 콩국수 "주문에서 빼기", 여름 다시 "불러오기"
- [ ] 폰/PC/.exe 에서 새 기능 동작 확인

## 배포 결과
(기록 예정)
